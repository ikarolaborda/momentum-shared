{
  "name": "@momentum/shared",
  "version": "1.0.0",
  "description": "Shared types, DTOs, constants, and utilities for the Momentum ticketing platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint \"src/**/*.ts\"",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "typescript": "^5.7.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.3.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0"
  }
}
